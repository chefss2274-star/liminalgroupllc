# Anthropic API key — required for AI summaries on the /audit page
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional: URL of a separately-deployed backend (leave blank to use the
# built-in Next.js API route at /api/audit/analyze)
NEXT_PUBLIC_API_URL=
